# Atlas Stack Status
Generated by scaffolder. Read this before writing any code.

## Verdict: GREENFIELD

Note: the project directory contained only bootstrap scaffolding (Dockerfile stub,
README, k8s/kustomization manifests, mockups, requirements, an empty `frontend/`
node_modules mount) — no application source. The enterprise template was freshly
scaffolded on top.

## Detected stack (from freshly scaffolded enterprise template)
- Runtime: NestJS (TypeScript) backend + Angular 19 frontend
- Internal API: tRPC (nestjs-trpc), end-to-end typed
- ORM: Prisma (PostgreSQL)
- Auth: none in template yet — plan requires full_auth (JWT), role `user`
- Health checks: present at GET /health (@nestjs/terminus)
- K8s service name: backend (per colossus.yaml), frontend served via nginx SPA fallback

## Rules for all agents
- Do not migrate the ORM (stay on Prisma)
- Do not replace the tRPC internal API with REST/GraphQL
- Do not change the framework (Angular 19 / NestJS)
- Only add features within the existing patterns
- Preserve `data-testid="app-ready"` on the Angular root component
