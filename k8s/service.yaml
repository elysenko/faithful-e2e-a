apiVersion: v1
kind: Service
metadata:
  name: faithful-e2e-a
  namespace: colossus-faithful-e2e-a
  labels:
    app: faithful-e2e-a
    managed-by: colossus
spec:
  type: ClusterIP
  selector:
    app: faithful-e2e-a
  ports:
    - port: 80
      targetPort: 80
